version: '3.8'

services:
  web:
    build: ./
    command: >
      sh -c "python manage.py migrate
      && python manage.py runserver 0.0.0.0:8000"
    volumes:
      - ./:/usr/src/MG_RMS/
    ports:
      - 8000:8000
    env_file:
      - ./.env.dev
